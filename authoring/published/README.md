# Published author content

Only material intentionally placed under `documents/` is an input to the TASK-004 compiler.

The compiler does not scan `authoring/` as a whole, an Obsidian vault, `private/`, `drafts/`, or this
README. See the parent authoring README for the source format and commands.
